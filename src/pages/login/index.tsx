import { Box, Heading, Text } from "@chakra-ui/react";
import { JSX } from "react";

const LoginPage: React.FC = (): JSX.Element => {
  return (
    <Box>
      <Heading>Taufik Hidayat</Heading>
    </Box>
  );
};

export default LoginPage;
